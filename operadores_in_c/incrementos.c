#include<stdio.h>

int main(void) {
  /* testando incrementos */
  int x = 0;

// atribuicao - o valor de x agora esta atribuido a variavel resultad + 1 /
  int resultado = x++;

  printf("\n Resultado = %d", resultado);
  printf("\n Valor nove de x: %d", x);

  return 0;
}
